# Устойчивость траектории к параметрам агрегации

Прогон от 17.08.2026 на `data/events_v1.5.jsonl` (280 событий, 55 месяцев).
Воспроизведение:

```bash
python run.py --sensitivity
```

Сетка: 240 конфигураций — `N_норм` ∈ {8, 10, 12, 15, 20}, `α` ∈ {0.2, 0.3, 0.4, 0.5},
`clip` ∈ {3, 5, 100}, четыре схемы весов значимости (линейная 1/2/3, равные, крутая 1/2/4,
пологая 1/1.5/2). База — то, что стоит в config.yaml: N=12, α=0.4, clip=5, линейная.

## Итог

| Ось | форма (ранг. корр., мин / медиана) | итог базы | итог по сетке | согласие знака | развороты |
|---|---|---|---|---|---|
| freedom_from | +0.91 / +0.98 | −0.54 | −1.03 … −0.13 | 78% | 12 мес. |
| freedom_for | +0.89 / +0.97 | +0.11 | +0.03 … +0.17 | 65% | 19 мес. |
| solidarity | +0.83 / +0.96 | +0.19 | +0.03 … +0.36 | 89% | 6 мес. |
| hope | +0.89 / +0.98 | +0.07 | +0.03 … +0.15 | 84% | 9 мес. |

**Форма устойчива, величина — нет.** Ранговая корреляция с базой не опускается ниже +0.83
ни в одной из 240 конфигураций: подъёмы и спады стоят на своих местах при любом разумном
выборе параметров. Но итоговый уровень `freedom_from` гуляет от −1.03 до −0.13 — восьмикратный
разброс. Отсюда прямое следствие: **абсолютные значения Level цитировать нельзя**, читаются
только отклонения от собственного базлайна. Это ровно то, чего требует METHODOLOGY §5,
и теперь это подтверждено численно, а не заявлено.

## Что на что влияет

Итоговый уровень при изменении одного параметра, остальные на базе:

| Параметр | freedom_from | solidarity |
|---|---|---|
| `N_норм` 8…20 | −0.81 … −0.33 | +0.11 … +0.28 |
| веса значимости | −0.63 … −0.23 | +0.09 … +0.19 |
| `α` 0.2…0.5 | −0.59 … −0.53 | +0.09 … +0.24 |
| `clip` 3…100 | −0.54 … −0.54 | +0.19 … +0.19 |

- **`N_норм` и схема весов — настоящие рычаги**, каждый двигает уровень в 2–3 раза.
- **`α` почти не рычаг**: на `freedom_from` разница между 0.2 и 0.5 — около 10%. Спор
  «0.3 или 0.4» решать не нужно, он ни на что не влияет.
- **`clip` при базовых параметрах не делает ничего** — значения совпадают до знака после
  запятой при 3, 5 и 100. По сетке он срабатывает в 28 конфигурациях из 240, и все они
  в углу с малым нормировщиком. На боевых параметрах порог недостижим.

## Две разные нестабильности знака

Развороты знака есть, но у них две несводимые причины, и лечатся они по-разному.

### 1. Мёртвая зона около нуля

Разворотные месяцы — это месяцы, где стрелка и так у нуля:

| Ось | медиана \|Level\| по всем месяцам | в разворотных | в устойчивых |
|---|---|---|---|
| freedom_from | 0.166 | **0.035** | 0.208 |
| freedom_for | 0.048 | **0.011** | 0.102 |
| solidarity | 0.046 | **0.011** | 0.050 |
| hope | 0.140 | **0.031** | 0.149 |

Разница в 5–10 раз. Доля месяцев, где все 240 конфигураций согласны по знаку, растёт с порогом:

| Порог \|Level\| | freedom_from | freedom_for | solidarity | hope |
|---|---|---|---|---|
| 0.00 (все месяцы) | 78% | 65% | 89% | 84% |
| 0.03 | 87% | 94% | 97% | 88% |
| 0.05 | 86% | **100%** | 96% | 96% |
| 0.08 | 87% | 100% | 94% | **100%** |

**Рекомендация: мёртвая зона ±0.05.** Ниже неё знак не читается — там разница между
«чуть выше нуля» и «чуть ниже» создаётся выбором нормировщика, а не событиями.
`freedom_for` и `hope` выше порога устойчивы полностью.

### 2. Инерция EMA — важнее первой

`freedom_from` не выходит на 100% согласия даже при \|Level\| ≥ 0.10, застревая на 87%.
Разбор упорных месяцев объясняет, почему:

| Месяц | Level базы | события месяца по оси |
|---|---|---|
| 2022-04 | **−0.127** | +1×w2, +1×w2 — оба **положительные** |
| 2022-08 | +0.050 | **нет ни одного** |
| 2024-05 | +0.144 | +1×w2 |

В апреле 2022 линия отрицательная, хотя оба события месяца положительные: уровень доезжает
на памяти о январе-2022 с его кластером −3. В августе 2022 по оси не произошло **ничего**,
а линия не в нуле. Знак в таких месяцах определяется тем, как быстро модель забывает шок,
то есть выбором `α` — и ничем больше.

Масштаб явления по всему ряду:

| Ось | месяцев, где Flow = 0, а Level ≠ 0 | из них Flow противоположен Level |
|---|---|---|
| freedom_from | 17 из 55 (31%) | 7 |
| freedom_for | 28 из 55 (51%) | 2 |
| solidarity | **35 из 55 (64%)** | 2 |
| hope | 9 из 55 (16%) | 7 |

**Две трети кривой solidarity и половина freedom_for — чистое затухание, а не наблюдение.**
Это прямое следствие разреженности осей (нулей 91% и 88%): мало событий → мало ненулевых
Flow → линия между ними просто едет вниз. Кривая выглядит непрерывным наблюдением,
которым не является.

Отдельно: на `freedom_from` и `hope` есть по 7 месяцев, где поле двинулось **против**
сглаженной линии — Flow вверх, Level вниз. Читать такой месяц по линии — прочитать его наоборот.

## Что с этим делать

1. **Не цитировать абсолютные значения Level.** Только направление и сравнение с собственным
   базлайном. Величина — артефакт нормировщика.
2. **Мёртвая зона ±0.05** — внутри неё знак не интерпретируется.
3. **Помечать месяцы инерции и мёртвую зону на дашборде.** Сделано, но не так, как
   планировалось. Фазовый портрет из 55 наложенных точек оказался нечитаемым в принципе:
   он прячет время, а читателю нужно именно оно. Попытка спасти его подписанными делениями,
   затенённым крестом и легендой сделала только плотнее. Портрет заменён **лентой квадрантов**:
   один прямоугольник — один месяц, слева направо, цвет — положение поля. Месяцы внутри
   мёртвой зоны получают отдельную категорию «у нуля» вместо названия квадранта, месяцы без
   событий по обеим осям — штриховку. Побочный результат замены: видно, что **квадрант не
   определён в 35 месяцах из 55** — на точечном графике это терялось.
4. **`clip` можно убирать** из конфигурации или задокументировать как страховку от выбросов,
   которая на текущих данных не срабатывает. Сейчас это параметр, создающий иллюзию настройки.
5. **Спор об `α` закрыт** — параметр ни на что не влияет в диапазоне 0.2–0.5. Настраивать,
   если понадобится, следует `N_норм` и схему весов.
6. **Для этапа 2 это важнее, чем для ретроспективы.** Дневной режим (`N_норм = 10`, `α = 0.1`)
   даст ещё более разреженные оси: событий в день меньше, чем в месяц, значит доля месяцев
   инерции вырастет. Прежде чем строить дневную кривую, стоит проверить эту же сетку на
   дневных параметрах — иначе на дашборде будет линия, у которой почти каждая точка
   унаследована, а не измерена.
